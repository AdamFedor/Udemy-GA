let example = 'test -> var';

let func = async (arg1) => {
    console.log(arg1);
}
func('hello');

let test = new Map();

let result = "hello";
// |> doublSay

// |> capitalize
// |> exclaim;

// the pipline fails, but the theory is there on npm install and add into babel config